/**
 * Supabase generated types.
 *
 * Regenerate after schema changes with:
 *   supabase gen types typescript --project-id <PROJECT_ID> > src/types/supabase.ts
 *
 * Until the Supabase project exists, this is a minimal stand-in so the
 * type-safe clients compile. Replace with the generated file in M1.
 */
export type Database = Record<string, never>;
